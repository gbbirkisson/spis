---
name: CI

on:
  workflow_call:
  pull_request:

env:
  VERSION: spis-v0.5.5

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Run test
        run: echo ${{ env.VERSION//spis-/ }}
